server {
    listen 80;

    server_name quran.bilalelhoudaigui.com;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }

    location /static/ {
        try_files $uri =404;
    }

    error_page 404 /index.html;
}